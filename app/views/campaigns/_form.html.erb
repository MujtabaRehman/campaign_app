<%= form_with(model: campaign) do |f| %>
  <% if campaign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(campaign.errors.count, "error") %> prohibited this Campaign from being saved:</h2>
      <ul>
        <% campaign.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
    <%end%>
  <div>
    <div class="field form-group">
      <% f.label :title %>
      <%= f.text_field :title, class: "form-control",placeholder:"Title"  %>
    </div>
    <div class="field form-group">
      <%f.label :purpose %>
      <%= f.text_field :purpose, class: "form-control",placeholder:"Purpose"  %>
    </div>
    <div class="field form-group ">
     <% f.label :estimated_time %>
      <%= f.select :estimated_time, Campaign.estimated_times.keys, {}, class:'form-control' %>
    </div>
    <div class="field form-group">
      <div class="col-sm-10">    
      <%= f.submit "Create" , class:"btn btn-info" %>   
      <%= link_to "Back", root_path , class:'btn btn-info'%>
   
    </div>
  </div>
<% end %>